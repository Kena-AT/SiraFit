global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "service"]
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 4h
  receiver: "default_receiver"

receivers:
  - name: "default_receiver"
    # Operators can attach webhook, Slack, PagerDuty, or email here
